[package]
name = "mcp-gitmem-proto"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0 OR MIT"
description = "MCP protocol types, tool schemas, and JSON-RPC glue for GitMem"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "0.8"
